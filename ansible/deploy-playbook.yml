---
- name: Production Deployment with Rolling Updates and Roles
  hosts: webserver
  become: yes
  gather_facts: yes
  serial: 1

  vars_files:
    - vault.yml

  roles:
    - docker_install
    - app_deploy
    - node_exporter
