---
- name: Production Deployment with Rolling Updates and Roles
  hosts: aws_ec2  # FIXED: Targets real-time discoverable cloud hosts group
  become: yes
  roles:
    - docker_install
    - app_deploy
    - node_exporter
